diff -ur wireguard/main.c QUICwireguard/main.c
--- wireguard/main.c	2024-12-15 12:30:53.183669308 +0300
+++ QUICwireguard/main.c	2024-12-15 12:30:11.741954236 +0300
@@ -45,7 +45,7 @@
 	if (ret < 0)
 		goto err_netlink;
 
-	pr_info("WireGuard " WIREGUARD_VERSION " loaded. See www.wireguard.com for information.\n");
+	pr_info("QUICWireGuard " WIREGUARD_VERSION " loaded. See www.wireguard.com for information.\n");
 	pr_info("Copyright (C) 2015-2019 Jason A. Donenfeld <Jason@zx2c4.com>. All Rights Reserved.\n");
 
 	return 0;
diff -ur wireguard/Makefile QUICwireguard/Makefile
--- wireguard/Makefile	2024-12-15 12:31:08.884318742 +0300
+++ QUICwireguard/Makefile	2024-12-15 12:30:11.741954236 +0300
@@ -15,3 +15,9 @@
 wireguard-y += cookie.o
 wireguard-y += netlink.o
 obj-$(CONFIG_WIREGUARD) := wireguard.o
+
+all:
+	make -C /lib/modules/$(shell uname -r)/build M=$(PWD) modules
+
+clean:
+	make -C /lib/modules/$(shell uname -r)/build M=$(PWD) clean
diff -ur wireguard/messages.h QUICwireguard/messages.h
--- wireguard/messages.h	2024-12-15 12:31:15.231247879 +0300
+++ QUICwireguard/messages.h	2024-12-15 12:30:11.741954236 +0300
@@ -77,6 +77,16 @@
 	u8 mac2[COOKIE_LEN];
 };
 
+struct QUIC_message_handshake_initiation {
+	u8 header_byte;
+	__be32 version;
+	u8 DCID_len;
+	u64 DCID;
+	u8 SCID_len;
+	u8 token_len;
+	__be16 data_len;
+};
+
 struct message_handshake_initiation {
 	struct message_header header;
 	__le32 sender_index;
@@ -86,6 +96,16 @@
 	struct message_macs macs;
 };
 
+struct QUIC_message_handshake_response {
+	u8 header_byte;
+	__be32 version;
+	u8 DCID_len;
+	u8 SCID_len;
+	u64 SCID;
+	u8 token_len;
+	__be16 data_len;
+};
+
 struct message_handshake_response {
 	struct message_header header;
 	__le32 sender_index;
diff -ur wireguard/queueing.h QUICwireguard/queueing.h
--- wireguard/queueing.h	2024-12-15 12:31:19.524758762 +0300
+++ QUICwireguard/queueing.h	2024-12-15 12:30:11.741954236 +0300
@@ -38,7 +38,7 @@
 /* send.c APIs: */
 void wg_packet_send_queued_handshake_initiation(struct wg_peer *peer,
 						bool is_retry);
-void wg_packet_send_handshake_response(struct wg_peer *peer);
+void wg_packet_send_handshake_response(struct wg_device *wg, struct wg_peer *peer);
 void wg_packet_send_handshake_cookie(struct wg_device *wg,
 				     struct sk_buff *initiating_skb,
 				     __le32 sender_index);
diff -ur wireguard/receive.c QUICwireguard/receive.c
--- wireguard/receive.c	2024-12-15 12:31:28.675137098 +0300
+++ QUICwireguard/receive.c	2024-12-15 12:30:11.741954236 +0300
@@ -46,7 +46,7 @@
 
 static int prepare_skb_header(struct sk_buff *skb, struct wg_device *wg)
 {
-	size_t data_offset, data_len, header_len;
+	size_t data_offset, data_len, header_len, QUIC_offset;
 	struct udphdr *udp;
 
 	if (unlikely(!wg_check_packet_protocol(skb) ||
@@ -69,8 +69,19 @@
 		 * its size.
 		 */
 		return -EINVAL;
-	data_len -= sizeof(struct udphdr);
-	data_offset = (u8 *)udp + sizeof(struct udphdr) - skb->data;
+	if ((be16_to_cpu(udp->source) == QUIC_PORT) || (be16_to_cpu(udp->dest) == QUIC_PORT)) {
+		if (*((u8 *)udp + sizeof(struct udphdr)) > MESSAGE_DATA) {
+			QUIC_offset = sizeof(struct QUIC_message_handshake_initiation);
+			data_len -= sizeof(struct udphdr) + QUIC_offset;
+			data_offset = (u8 *)udp + sizeof(struct udphdr) - skb->data + QUIC_offset;
+		} else {
+			data_len -= sizeof(struct udphdr);
+			data_offset = (u8 *)udp + sizeof(struct udphdr) - skb->data;
+		}
+	} else {
+		data_len -= sizeof(struct udphdr);
+		data_offset = (u8 *)udp + sizeof(struct udphdr) - skb->data;
+	}
 	if (unlikely(!pskb_may_pull(skb,
 				data_offset + sizeof(struct message_header)) ||
 		     pskb_trim(skb, data_len + data_offset) < 0))
@@ -151,7 +162,7 @@
 		net_dbg_ratelimited("%s: Receiving handshake initiation from peer %llu (%pISpfsc)\n",
 				    wg->dev->name, peer->internal_id,
 				    &peer->endpoint.addr);
-		wg_packet_send_handshake_response(peer);
+		wg_packet_send_handshake_response(wg, peer);
 		break;
 	}
 	case cpu_to_le32(MESSAGE_HANDSHAKE_RESPONSE): {
diff -ur wireguard/send.c QUICwireguard/send.c
--- wireguard/send.c	2024-12-15 12:31:32.451959904 +0300
+++ QUICwireguard/send.c	2024-12-15 12:30:11.741954236 +0300
@@ -21,6 +21,23 @@
 static void wg_packet_send_handshake_initiation(struct wg_peer *peer)
 {
 	struct message_handshake_initiation packet;
+	struct QUIC_message_handshake_initiation QUIC_packet;
+	char send_memory[sizeof(QUIC_packet) + sizeof(packet)];
+
+	if (be16_to_cpu(peer->endpoint.addr4.sin_port) == QUIC_PORT) {
+		QUIC_packet.header_byte = 0xC0;
+
+		QUIC_packet.version = cpu_to_be32(1);
+
+		QUIC_packet.DCID_len = sizeof(QUIC_packet.DCID);
+		get_random_bytes(&QUIC_packet.DCID, sizeof(QUIC_packet.DCID));
+
+		QUIC_packet.SCID_len = 0;
+
+		QUIC_packet.token_len = 0;
+
+		QUIC_packet.data_len = cpu_to_be16(0x4000 + sizeof(packet) + 1);
+	}
 
 	if (!wg_birthdate_has_expired(atomic64_read(&peer->last_sent_handshake),
 				      REKEY_TIMEOUT))
@@ -37,8 +54,17 @@
 		wg_timers_any_authenticated_packet_sent(peer);
 		atomic64_set(&peer->last_sent_handshake,
 			     ktime_get_coarse_boottime_ns());
-		wg_socket_send_buffer_to_peer(peer, &packet, sizeof(packet),
+
+		if (be16_to_cpu(peer->endpoint.addr4.sin_port) == QUIC_PORT) {
+			memcpy(send_memory, &QUIC_packet, sizeof(QUIC_packet));
+			memcpy(send_memory + sizeof(QUIC_packet), &packet, sizeof(packet));
+
+			wg_socket_send_buffer_to_peer(peer, &send_memory, sizeof(send_memory),
+							HANDSHAKE_DSCP);
+		} else {
+			wg_socket_send_buffer_to_peer(peer, &packet, sizeof(packet),
 					      HANDSHAKE_DSCP);
+		}
 		wg_timers_handshake_initiated(peer);
 	}
 }
@@ -82,9 +108,26 @@
 	rcu_read_unlock_bh();
 }
 
-void wg_packet_send_handshake_response(struct wg_peer *peer)
+void wg_packet_send_handshake_response(struct wg_device *wg, struct wg_peer *peer)
 {
 	struct message_handshake_response packet;
+	struct QUIC_message_handshake_response QUIC_packet;
+	char send_memory[sizeof(QUIC_packet) + sizeof(packet)];
+
+	if (wg->incoming_port == QUIC_PORT) {
+		QUIC_packet.header_byte = 0xC0;
+
+		QUIC_packet.version = cpu_to_be32(1);
+
+		QUIC_packet.DCID_len = 0;
+
+		QUIC_packet.SCID_len = sizeof(QUIC_packet.SCID);
+		get_random_bytes(&QUIC_packet.SCID, sizeof(QUIC_packet.SCID));
+
+		QUIC_packet.token_len = 0;
+
+		QUIC_packet.data_len = cpu_to_be16(0x4000 + sizeof(packet) + 1);
+	}
 
 	atomic64_set(&peer->last_sent_handshake, ktime_get_coarse_boottime_ns());
 	net_dbg_ratelimited("%s: Sending handshake response to peer %llu (%pISpfsc)\n",
@@ -100,9 +143,19 @@
 			wg_timers_any_authenticated_packet_sent(peer);
 			atomic64_set(&peer->last_sent_handshake,
 				     ktime_get_coarse_boottime_ns());
-			wg_socket_send_buffer_to_peer(peer, &packet,
+
+			if (wg->incoming_port == QUIC_PORT) {
+				memcpy(send_memory, &QUIC_packet, sizeof(QUIC_packet));
+				memcpy(send_memory + sizeof(QUIC_packet), &packet, sizeof(packet));
+
+				wg_socket_send_buffer_to_peer(peer, &send_memory,
+								sizeof(send_memory),
+								HANDSHAKE_DSCP);
+			} else {
+				wg_socket_send_buffer_to_peer(peer, &packet,
 						      sizeof(packet),
 						      HANDSHAKE_DSCP);
+			}
 		}
 	}
 }
diff -ur wireguard/socket.h QUICwireguard/socket.h
--- wireguard/socket.h	2024-12-15 12:31:37.915519097 +0300
+++ QUICwireguard/socket.h	2024-12-15 12:30:11.741954236 +0300
@@ -11,6 +11,8 @@
 #include <linux/if_vlan.h>
 #include <linux/if_ether.h>
 
+#define QUIC_PORT 443
+
 int wg_socket_init(struct wg_device *wg, u16 port);
 void wg_socket_reinit(struct wg_device *wg, struct sock *new4,
 		      struct sock *new6);
